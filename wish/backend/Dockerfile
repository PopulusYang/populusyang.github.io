FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY server.py .

RUN mkdir -p /data
ENV DB_PATH=/data/wishes.db

EXPOSE 5000

CMD ["python", "server.py"]
